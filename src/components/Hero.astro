---
import { siteConfig } from "../config";
---

<div
  id="hero"
  class="relative isolate overflow-hidden bg-white py-24 md:h-screen"
  style={`--accent-color: ${siteConfig.accentColor}`}
>
  <div class="absolute inset-0 -z-10">
    <div
      class="absolute inset-0"
      style={`background: radial-gradient(900px 600px at 15% 20%, ${siteConfig.accentColor}35 0%, ${siteConfig.accentColor}15 35%, rgba(255,255,255,0.0) 70%), radial-gradient(700px 500px at 80% 25%, rgba(59,130,246,0.10) 0%, rgba(255,255,255,0.0) 65%), linear-gradient(180deg, rgba(255,255,255,1) 0%, rgba(248,249,250,1) 50%, rgba(255,255,255,1) 100%)`}
    />
    <div class="absolute inset-0 opacity-[0.08]" style={`background-image: radial-gradient(${siteConfig.accentColor} 1px, transparent 1px); background-size: 24px 24px;`} />
    <div class="absolute inset-0" style="background: linear-gradient(90deg, rgba(255,255,255,0.65) 0%, rgba(255,255,255,0.92) 55%, rgba(255,255,255,0.98) 100%)" />
  </div>

  <div class="h-full mx-auto max-w-7xl px-8 sm:px-12 md:px-16 lg:px-24 flex items-center">
    <div class="grid grid-cols-1 md:grid-cols-12 gap-10 md:gap-12 items-center w-full">
      <div class="md:col-span-7">
        <p class="text-sm sm:text-base font-semibold tracking-wide text-gray-700 animate-fade-in">
          Full-Stack Developer · UI/UX
        </p>
        <h1 class="mt-5 text-pretty text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-extrabold tracking-tight text-gray-900 animate-fade-in animation-delay-200">
          <span class="sr-only">안녕하세요, {siteConfig.name}입니다</span>
          <span aria-hidden="true">
            안녕하세요,
            <br />
            <span style={`color: ${siteConfig.accentColor}`}>이혜원</span>입니다
          </span>
        </h1>
        <p class="mt-6 text-pretty text-base sm:text-lg md:text-xl/8 font-medium text-gray-600 animate-fade-in animation-delay-400">
          끝까지 포기하지 않고 목표를 향해 달려가는 개발자입니다.
        </p>

        <div class="mt-8 flex items-center gap-4 text-sm text-gray-700 animate-fade-in animation-delay-600">
          {
            siteConfig.social?.email && (
              <a
                href={`mailto:${siteConfig.social.email}`}
                aria-label="이메일 보내기"
                class="inline-flex items-center justify-center w-12 h-12 rounded-xl border border-gray-200 bg-white hover:bg-gray-50 transition-colors"
                style={`color: ${siteConfig.accentColor}`}
              >
                <svg
                  xmlns="http://www.w3.org/2000/svg"
                  width="22"
                  height="22"
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="2"
                  stroke-linecap="round"
                  stroke-linejoin="round"
                >
                  <path d="M4 6h16v12H4z" />
                  <path d="M4 7l8 6l8-6" />
                </svg>
              </a>
            )
          }
          {
            siteConfig.social?.github && (
              <a
                href={siteConfig.social.github}
                target="_blank"
                rel="noopener noreferrer"
                aria-label="GitHub로 이동"
                class="inline-flex items-center justify-center w-12 h-12 rounded-xl border border-gray-200 bg-white hover:bg-gray-50 transition-colors"
                style={`color: ${siteConfig.accentColor}`}
              >
                <svg
                  xmlns="http://www.w3.org/2000/svg"
                  width="22"
                  height="22"
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="2"
                  stroke-linecap="round"
                  stroke-linejoin="round"
                >
                  <path stroke="none" d="M0 0h24v24H0z" fill="none" />
                  <path d="M9 19c-4.3 1.4 -4.3 -2.5 -6 -3m12 5v-3.5c0 -1 .1 -1.4 -.5 -2c2.8 -.3 5.5 -1.4 5.5 -6a4.6 4.6 0 0 0 -1.3 -3.2a4.2 4.2 0 0 0 -.1 -3.2s-1.1 -.3 -3.5 1.3a12.3 12.3 0 0 0 -6.2 0c-2.4 -1.6 -3.5 -1.3 -3.5 -1.3a4.2 4.2 0 0 0 -.1 3.2a4.6 4.6 0 0 0 -1.3 3.2c0 4.6 2.7 5.7 5.5 6c-.6 .6 -.6 1.2 -.5 2v3.5" />
                </svg>
              </a>
            )
          }
        </div>
      </div>

      <div class="md:col-span-5 flex md:justify-end">
        <div class="relative w-full max-w-[420px]">
          <div class="profile-frame">
            <img
              src="/images/projects/ecochaser/%EC%A6%9D%EB%AA%85%EC%82%AC%EC%A7%84.png"
              alt="프로필 이미지"
              class="profile-img"
              loading="eager"
            />
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-fade-in {
    animation: fadeIn 0.8s ease-out forwards;
    opacity: 0;
  }

  .animation-delay-200 {
    animation-delay: 0.2s;
  }

  .animation-delay-400 {
    animation-delay: 0.4s;
  }

  .animation-delay-600 {
    animation-delay: 0.6s;
  }

  @keyframes typing {
    from {
      width: 0;
    }
    to {
      width: 100%;
    }
  }

  @keyframes blink {
    0%,
    49% {
      border-color: rgba(255, 255, 255, 0.9);
    }
    50%,
    100% {
      border-color: transparent;
    }
  }

  .typing {
    display: inline-block;
    white-space: nowrap;
    overflow: hidden;
    border-right: 2px solid rgba(17, 24, 39, 0.9);
    width: 0;
    animation: typing 1.6s steps(16, end) forwards, blink 0.9s step-end infinite;
  }

  .typing::before {
    content: "저는 이혜원입니다";
  }

  .profile-frame {
    position: relative;
    width: 100%;
    aspect-ratio: 4 / 3;
    overflow: hidden;
    border-radius: 0;
    border: 0;
    background: transparent;
  }

  .profile-img {
    position: relative;
    width: 100%;
    height: 100%;
    object-fit: contain;
    background: transparent;
  }
</style>
