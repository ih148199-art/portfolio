---
import { siteConfig } from "../config";
---

<section id="contact" class="py-16 sm:py-20 mb-20">
  <div class="max-w-[1200px] mx-auto px-6 sm:px-8">
    <div>
      <h2 class="text-2xl sm:text-3xl font-bold text-gray-900">Connect me</h2>
      <div
        class="w-14 h-1 mt-3 rounded-full"
        style={`background-color: ${siteConfig.accentColor}`}
      />
      <p class="mt-4 text-sm sm:text-base text-gray-600">
        프로젝트/협업 문의는 아래로 편하게 연락주세요.
      </p>
    </div>

    <div class="mt-10 grid grid-cols-1 sm:grid-cols-2 gap-4">
      <a
        href={`mailto:${siteConfig.social.email}`}
        aria-label="이메일 보내기"
        class="group rounded-2xl border border-gray-200 bg-white p-6 shadow-sm hover:shadow-md transition-shadow"
      >
        <div class="flex items-center justify-between gap-4">
          <div class="min-w-0">
            <p class="text-sm font-semibold text-gray-900">Email</p>
            <p class="mt-2 text-sm text-gray-600 break-all">{siteConfig.social.email}</p>
          </div>
          <div
            class="w-12 h-12 rounded-xl flex items-center justify-center"
            style={`background-color: ${siteConfig.accentColor}10; color: ${siteConfig.accentColor}`}
          >
            <svg
              xmlns="http://www.w3.org/2000/svg"
              width="20"
              height="20"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
            >
              <path d="M4 6h16v12H4z" />
              <path d="M4 7l8 6l8-6" />
            </svg>
          </div>
        </div>
      </a>

      <a
        href={siteConfig.social.github}
        target="_blank"
        rel="noopener noreferrer"
        aria-label="GitHub로 이동"
        class="group rounded-2xl border border-gray-200 bg-white p-6 shadow-sm hover:shadow-md transition-shadow"
      >
        <div class="flex items-center justify-between gap-4">
          <div class="min-w-0">
            <p class="text-sm font-semibold text-gray-900">GitHub</p>
            <p class="mt-2 text-sm text-gray-600 break-all">{siteConfig.social.github}</p>
          </div>
          <div
            class="w-12 h-12 rounded-xl flex items-center justify-center"
            style={`background-color: ${siteConfig.accentColor}10; color: ${siteConfig.accentColor}`}
          >
            <svg
              xmlns="http://www.w3.org/2000/svg"
              width="20"
              height="20"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
            >
              <path stroke="none" d="M0 0h24v24H0z" fill="none" />
              <path d="M9 19c-4.3 1.4 -4.3 -2.5 -6 -3m12 5v-3.5c0 -1 .1 -1.4 -.5 -2c2.8 -.3 5.5 -1.4 5.5 -6a4.6 4.6 0 0 0 -1.3 -3.2a4.2 4.2 0 0 0 -.1 -3.2s-1.1 -.3 -3.5 1.3a12.3 12.3 0 0 0 -6.2 0c-2.4 -1.6 -3.5 -1.3 -3.5 -1.3a4.2 4.2 0 0 0 -.1 3.2a4.6 4.6 0 0 0 -1.3 3.2c0 4.6 2.7 5.7 5.5 6c-.6 .6 -.6 1.2 -.5 2v3.5" />
            </svg>
          </div>
        </div>
      </a>
    </div>

    <div class="mt-8 rounded-2xl border border-gray-200 bg-white p-6 sm:p-7 shadow-sm">
      <p class="text-lg font-bold text-gray-900 text-center">메시지 보내기</p>
      <form id="contact-form" class="mt-6 grid grid-cols-1 gap-4">
        <label class="block">
          <span class="text-xs font-semibold text-gray-700">이름</span>
          <input
            name="name"
            type="text"
            required
            class="mt-2 w-full rounded-xl border border-gray-200 bg-white px-4 py-3 text-gray-900 placeholder-gray-400 outline-none focus:ring-2 focus:ring-[var(--accent-color)]"
            style={`--accent-color: ${siteConfig.accentColor}`}
            placeholder="홍길동"
          />
        </label>

        <label class="block">
          <span class="text-xs font-semibold text-gray-700">이메일</span>
          <input
            name="email"
            type="email"
            required
            class="mt-2 w-full rounded-xl border border-gray-200 bg-white px-4 py-3 text-gray-900 placeholder-gray-400 outline-none focus:ring-2 focus:ring-[var(--accent-color)]"
            style={`--accent-color: ${siteConfig.accentColor}`}
            placeholder="example@email.com"
          />
        </label>

        <label class="block">
          <span class="text-xs font-semibold text-gray-700">메시지</span>
          <textarea
            name="message"
            rows={5}
            required
            class="mt-2 w-full rounded-xl border border-gray-200 bg-white px-4 py-3 text-gray-900 placeholder-gray-400 outline-none focus:ring-2 focus:ring-[var(--accent-color)]"
            style={`--accent-color: ${siteConfig.accentColor}`}
            placeholder="메시지를 입력해주세요..."
          />
        </label>

        <button
          type="submit"
          class="mt-2 w-full rounded-xl py-3 font-semibold text-white"
          style={`background-color: ${siteConfig.accentColor}`}
        >
          전송하기
        </button>
      </form>
    </div>
  </div>

  <script>
    const form = document.getElementById('contact-form');
    form?.addEventListener('submit', (e) => {
      e.preventDefault();
      const target = e.currentTarget;
      if (!(target instanceof HTMLFormElement)) return;
      const data = new FormData(target);
      const name = String(data.get('name') ?? '').trim();
      const email = String(data.get('email') ?? '').trim();
      const message = String(data.get('message') ?? '').trim();

      const subject = encodeURIComponent(`[Portfolio] ${name}님의 메시지`);
      const body = encodeURIComponent(`이름: ${name}\n이메일: ${email}\n\n메시지:\n${message}`);
      const to = `${siteConfig.social.email}`;
      window.location.href = `mailto:${to}?subject=${subject}&body=${body}`;
    });
  </script>
</section>
