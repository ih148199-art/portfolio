---
import { siteConfig } from "../config";
const featuredProject =
  siteConfig.projects?.find((p) => p.name === "EcoChaser") ?? siteConfig.projects?.[0];
const hasProjects = Boolean(featuredProject);
const linkProps = featuredProject?.link
  ? {
      href: featuredProject.link,
      target: "_blank",
      rel: "noopener noreferrer",
    }
  : {};

 const presentationLink = (featuredProject as any)?.presentation as
  | string
  | undefined;

 const demoVideoSrc = "/videos/portfolio_highlight.mp4";

 const coreDev = [
   "HTML/CSS/JS",
   "Node.js",
   "Cloudflare (Functions/D1)",
   "Cloudflare Pages",
 ];
 const designStack = ["Figma (UI/UX)"];
 const collaboration = ["Git/GitHub", "Notion", "Discord"];
 const aiEfficiency = [
   "ChatGPT (Refactoring)",
   "Midjourney (Assets)",
   "Notion AI (Docs)",
 ];
---

{
  hasProjects && (
    <section
      id="projects"
      class="py-16 sm:py-20 mb-20"
      style={`font-family: Pretendard, "Noto Sans KR", ui-sans-serif, system-ui, -apple-system, "Segoe UI", Arial, sans-serif;`}
    >
      <div class="max-w-[1200px] mx-auto px-6 sm:px-8">
        <div>
          <h2 class="text-2xl sm:text-3xl font-bold text-[#0B1B3A]">프로젝트</h2>
          <div
            class="w-14 h-1 mt-3 rounded-full"
            style={`background-color: ${siteConfig.accentColor}`}
          />
        </div>

        <div class="mt-10 rounded-3xl border border-white/10 bg-[#0B1220] shadow-[0_28px_90px_rgba(0,0,0,0.50)] overflow-hidden">
          <div class="relative">
            <div class="aspect-video w-full overflow-hidden">
              <video
                class="w-full h-full object-cover"
                src={presentationLink ?? demoVideoSrc}
                autoplay
                loop
                muted
                playsinline
                preload="metadata"
                poster="/images/projects/ecochaser/background.png"
              />
            </div>
            <div class="absolute inset-0 bg-gradient-to-b from-black/35 via-black/10 to-[#0B1220]" aria-hidden="true" />
          </div>

          <div class="px-6 sm:px-10 py-8 sm:py-10 text-white">
            <div class="flex items-start justify-between gap-4">
              <div class="min-w-0">
                <h3 class="text-2xl sm:text-3xl font-extrabold tracking-tight">
                  {featuredProject?.name}
                </h3>
                <p class="mt-2 text-sm text-white/60">
                  Team Project · Web Full-Stack · Serverless
                </p>
              </div>

              <div class="flex items-center gap-2 shrink-0">
                {featuredProject?.github && (
                  <a
                    href={featuredProject.github}
                    target="_blank"
                    rel="noopener noreferrer"
                    aria-label="GitHub"
                    class="inline-flex items-center justify-center w-10 h-10 rounded-xl border border-white/15 bg-white/5 hover:bg-white/10 transition-colors"
                    style={`color: ${siteConfig.accentColor}`}
                  >
                    <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                      <path stroke="none" d="M0 0h24v24H0z" fill="none" />
                      <path d="M9 19c-4.3 1.4 -4.3 -2.5 -6 -3m12 5v-3.5c0 -1 .1 -1.4 -.5 -2c2.8 -.3 5.5 -1.4 5.5 -6a4.6 4.6 0 0 0 -1.3 -3.2a4.2 4.2 0 0 0 -.1 -3.2s-1.1 -.3 -3.5 1.3a12.3 12.3 0 0 0 -6.2 0c-2.4 -1.6 -3.5 -1.3 -3.5 -1.3a4.2 4.2 0 0 0 -.1 3.2a4.6 4.6 0 0 0 -1.3 3.2c0 4.6 2.7 5.7 5.5 6c-.6 .6 -.6 1.2 -.5 2v3.5" />
                    </svg>
                  </a>
                )}

                {featuredProject?.link && (
                  <a
                    {...linkProps}
                    aria-label="Live Demo"
                    class="inline-flex items-center justify-center w-10 h-10 rounded-xl border border-white/15 bg-white/5 hover:bg-white/10 transition-colors"
                    style={`color: ${siteConfig.accentColor}`}
                  >
                    <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                      <path d="M10 13a5 5 0 0 0 7.54.54l3-3a5 5 0 0 0-7.07-7.07l-1.72 1.71" />
                      <path d="M14 11a5 5 0 0 0-7.54-.54l-3 3a5 5 0 0 0 7.07 7.07l1.71-1.71" />
                    </svg>
                  </a>
                )}
              </div>
            </div>

            <p class="mt-6 text-sm sm:text-base text-white/75 leading-relaxed max-w-[72ch]">
              지역별 분리수거 규칙을 재밌게 학습하는 게이미피케이션 웹앱 서비스

             
            </p>

            <div class="mt-5 flex flex-wrap gap-2">
              {[
                "Cloudflare",
                "D1",
                "HTML",
                "CSS",
                "JavaScript",
                "Functions",
                "Node.js",
                "Express",
              ].map((tag) => (
                <span class="px-3 py-1 rounded-full border border-white/10 bg-white/5 text-xs font-semibold text-white/80">
                  {tag}
                </span>
              ))}
            </div>
          </div>
        </div>

        <div class="mt-12 grid grid-cols-1 md:grid-cols-2 gap-6">
          <div class="rounded-3xl border border-gray-200 bg-white p-6 sm:p-8 shadow-[0_10px_30px_rgba(15,23,42,0.06)]">
            <h3 class="text-lg sm:text-xl font-extrabold text-gray-900">
              Why &amp; Target
            </h3>
            <p class="mt-4 text-sm sm:text-base text-gray-700 leading-relaxed text-left">
              복잡한 분리수거 규칙을 어려워하는
              <span class="font-extrabold" style={`color: ${siteConfig.accentColor}`}> 유학생/신입생</span>
              을 위해 만든 러너형 게임
            </p>
          </div>

          <div class="rounded-3xl border border-gray-200 bg-white p-6 sm:p-8 shadow-[0_10px_30px_rgba(15,23,42,0.06)]">
            <h3 class="text-lg sm:text-xl font-extrabold text-gray-900">
              나의 기여도
            </h3>
            <ul class="mt-4 space-y-2 text-sm sm:text-base text-gray-700 text-left">
              <li class="flex gap-2">
                <span class="mt-[2px] text-gray-400">•</span>
                <span>Web Full-Stack (프론트/백엔드/배포)</span>
              </li>
              <li class="flex gap-2">
                <span class="mt-[2px] text-gray-400">•</span>
                <span>Cloudflare Functions(API) + D1(DB) 기반 설계/구현</span>
              </li>
              <li class="flex gap-2">
                <span class="mt-[2px] text-gray-400">•</span>
                <span>Admin 시스템 구축 (관리/삭제/운영 기능)</span>
              </li>
            </ul>
          </div>
        </div>

        <div class="mt-12">
          <div class="rounded-3xl border border-slate-200 bg-[#F1F6FF] px-6 sm:px-10 py-10">
            <h3 class="text-xl sm:text-2xl font-extrabold text-gray-900 text-center">
              Skills & Tools
            </h3>
            <div class="mt-8 grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
              <div class="rounded-2xl border border-slate-200 bg-white/70 p-5">
                <p class="text-sm font-extrabold text-gray-900">🧱 Core Dev</p>
                <div class="mt-3 flex flex-wrap gap-2">
                  {coreDev.map((t) => (
                    <span class="px-3 py-1 rounded-full border border-slate-200 bg-white text-xs font-semibold text-gray-700">
                      {t}
                    </span>
                  ))}
                </div>
              </div>

              <div class="rounded-2xl border border-slate-200 bg-white/70 p-5">
                <p class="text-sm font-extrabold text-gray-900">🎨 Design</p>
                <div class="mt-3 flex flex-wrap gap-2">
                  {designStack.map((t) => (
                    <span class="px-3 py-1 rounded-full border border-slate-200 bg-white text-xs font-semibold text-gray-700">
                      {t}
                    </span>
                  ))}
                </div>
              </div>

              <div class="rounded-2xl border border-slate-200 bg-white/70 p-5">
                <p class="text-sm font-extrabold text-gray-900">🤝 Collaboration</p>
                <div class="mt-3 flex flex-wrap gap-2">
                  {collaboration.map((t) => (
                    <span class="px-3 py-1 rounded-full border border-slate-200 bg-white text-xs font-semibold text-gray-700">
                      {t}
                    </span>
                  ))}
                </div>
              </div>

              <div class="rounded-2xl border border-slate-200 bg-white/70 p-5">
                <p class="text-sm font-extrabold text-gray-900">⚡ AI Efficiency</p>
                <div class="mt-3 flex flex-wrap gap-2">
                  {aiEfficiency.map((t) => (
                    <span class="px-3 py-1 rounded-full border border-slate-200 bg-white text-xs font-semibold text-gray-700">
                      {t}
                    </span>
                  ))}
                </div>
              </div>
            </div>
          </div>
        </div>

        <div class="mt-12">
          <div class="rounded-3xl border border-gray-200 bg-white p-6 sm:p-10 shadow-[0_16px_50px_rgba(15,23,42,0.10)]">
            <div class="text-center">
              <h3 class="text-2xl sm:text-3xl font-extrabold text-gray-900">
                문제점 & 해결방안
              </h3>
              <div
                class="w-16 h-1 mt-4 mx-auto rounded-full"
                style={`background-color: ${siteConfig.accentColor}`}
              />
            </div>

            <div class="mt-8 grid grid-cols-1 md:grid-cols-2 gap-4 md:gap-0 text-left md:divide-x md:divide-gray-200">
              <div class="md:pr-6">


                <div class="mt-4 space-y-3">
                  <div class="rounded-2xl border border-red-200 bg-red-50 p-4 sm:p-5">
                    <p class="text-sm sm:text-base font-extrabold text-gray-900">
                      🚨 Problem
                    </p>
                    <p class="mt-2 text-sm sm:text-base text-gray-700 leading-relaxed">
                      실시간 랭킹 집계 지연(Latency) 및 욕설 등 악성 데이터 노출 위험.
                    </p>
                  </div>

                  <div class="rounded-2xl border border-amber-200 bg-amber-50 p-4 sm:p-5">
                    <p class="text-sm sm:text-base font-extrabold text-gray-900">
                      💡 Approach
                    </p>
                    <p class="mt-2 text-sm sm:text-base text-gray-700 leading-relaxed">
                      단순 INSERT 방식의 비효율성(데이터 중복) 인지 →
                      <span class="font-semibold"> 무결성 보장 로직</span> 필요성 도출.
                    </p>
                  </div>
                </div>
              </div>

              <div class="md:pl-6 mt-4 md:mt-0">
      
                <div class="mt-4 space-y-3">
                  <div class="rounded-2xl border border-sky-200 bg-sky-50 p-4 sm:p-5">
                    <p class="text-sm sm:text-base font-extrabold text-gray-900">
                      🛠️ Solution
                    </p>
                    <p class="mt-2 text-sm sm:text-base text-gray-700 leading-relaxed">
                      <span class="font-semibold">Upsert쿼리</span>로 점수 갱신 로직 최적화 &amp;
                      관리자용 <span class="font-semibold">Hard Delete API</span> 개발.
                    </p>
                  </div>

                  <div class="rounded-2xl border border-emerald-200 bg-emerald-50 p-4 sm:p-5">
                    <p class="text-sm sm:text-base font-extrabold text-gray-900">
                      ✅ Result
                    </p>
                    <p class="mt-2 text-sm sm:text-base text-gray-700 leading-relaxed">
                      대규모 트래픽에서도 오차 없는 <span class="font-semibold">실시간 랭킹 시스템</span> 및 운영 효율성 확보.
                    </p>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>
  )
}
